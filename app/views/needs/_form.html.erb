<%= form_for(@need) do |f| %>
  <% if @need.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@need.errors.count, "error") %> prohibited this need from being saved:</h2>

      <ul>
      <% @need.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :desc, 'Description' %><br />
    <%= f.text_area :desc,
                    :rows => 5 %>
  </div>
  <div class="field">
    <%= f.label :date %><br />
    <%= f.date_select :date %>
  </div>
  <div class="field">
    <%= f.label :time %><br />
    <%= f.time_select :time, {:minute_step => 15, :ampm => true, :include_blank => true, :ignore_date=>true, :default => nil} %>
  </div>


  <div class="slots">
    <h4>Sign-Up Slots</h4>
    <button id="new_slot" class="btn btn-success">
      Add Slot
    </button>
    <%= f.fields_for :slots do |s| %>
    <div class ="slot">
      <%= link_to 'Remove Slot', s.object, :class=> 'btn btn-danger remove_slot', method: :delete%>
      <%= s.text_field :name %>
    </div>
  <% end %>
  </div>

  <div class="actions">
    <%= f.submit 'Post Need', :class => 'btn submit btn-primary', :id => 'post_need' %>
  </div>
<% end %>
